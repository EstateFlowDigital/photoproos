import { PostComposer } from "./post-composer";

export const metadata = {
  title: "Post Composer | Marketing Studio",
  description: "Create social media posts with platform-accurate previews",
};

export default function ComposerPage() {
  return <PostComposer />;
}
